module.exports = {
  'secret': 'hellohappypeople',
  'database': 'mongodb://localhost/test'
};

// Not sure if this is necessary, or at least, the database is not necessary
// since we are hosting on MongoDB
